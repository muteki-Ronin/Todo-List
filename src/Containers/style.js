import { createUseStyles } from "react-jss";

export const useStyle = createUseStyles({
  container: {
    border: "3px solid rgba(3, 115, 7, 0.5)",
    borderRadius: "15px",
    backgroundColor: "rgba(223, 224, 213, 0.2)",
    marginTop: "20px",
    padding: "30px",
  },
});
